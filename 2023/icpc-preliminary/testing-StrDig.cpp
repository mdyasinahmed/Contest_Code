#include<bits/stdc++.h>
using namespace std;


int main() {
    string input;
    cin >> input;

    string numStr;

    for(char ch : input) {
        if(isdigit(ch)) {
            numStr += ch;
        }
    }

    cout << numStr;





    return 0;
}



overs = (float)input.size()/6;
        cout << overs;