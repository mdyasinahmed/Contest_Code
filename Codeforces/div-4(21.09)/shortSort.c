#include<stdio.h>
#include<string.h>

int main() {
    int t;
    char s;
    scanf("%d", &t);

    while(t--) {
        
        scanf("%s", &s);
    }

    for(int i=0; i<s;i++){

    }
    printf("%s", s);
}